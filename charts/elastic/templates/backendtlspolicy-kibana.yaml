# BackendTLSPolicy is needed to accept the TLS certificate auto-generated by the eck-stack chart
apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: kibana
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chart.labels" . | nindent 4 }}
spec:
  targetRefs:
  - group: ""
    kind: Service
    name: elastic-eck-kibana-kb-http
    sectionName: https
  validation:
    caCertificateRefs:
    - group: ""
      kind: Secret
      name: elastic-eck-kibana-kb-http-certs-public
    hostname: elastic-eck-kibana-kb-http.{{ .Release.Namespace }}.svc
---